models represent collections in mongodb
schema represent structure of our data object
MONGOOSE:adds a layer of methods to easily save,retrieve,edit and delete data froom mongodb
allows us to create our models and schema easily
so use as: npm install mongoose 
//created a sep folder for our model




ERROR HANDLING:(when we not given a required field in form)
//create a middleware for error handling 
to access this created middleware use next in catch method 



PUT METHOD:
// update a ninja in the db
router.put("/ninjas/:id", function (req, res, next) {
  Ninja.findByIdAndUpdate({ _id: req.params.id }, req.body)
    .then(function () {
      Ninja.findOne({ _id: req.params.id }).then(function (ninja) {
        res.send(ninja);
      });
    })
    .catch(next);
});
// Ninja.findByIdAndUpdate({_id: req.params.id}, req.body, {new: true}).then(function(ninja){
//   res.send(ninja);
// });
// even this works